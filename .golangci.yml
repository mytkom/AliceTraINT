run:
  timeout: 5m

linters:
  enable:
    - govet
    - gofmt
    - unused
    - staticcheck
    - errcheck
    - goconst
    - gosimple

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
  exclude-use-default: false

